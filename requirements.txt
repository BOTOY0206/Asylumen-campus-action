# 基础Web框架
flask==3.0.0
# 轻量版YOLO，避免依赖冲突
ultralytics==8.3.0
# 无GUI OpenCV，适配CI
opencv-python-headless==4.10.0.84
# 兼容Python 3.13的numpy版本
numpy==2.0.0
# WSGI服务器
gunicorn==22.0.0
# 解决setuptools缺失问题
setuptools==70.0.0
wheel==0.43.0